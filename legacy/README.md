# Legacy modules

These modules are retained for audit and migration reference only. They are not imported by the production entrypoint because they contain development defaults, simulated payments, or unauthenticated mutation routes.
